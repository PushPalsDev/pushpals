{
  "name": "remotebuddy",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch --no-clear-screen src/remotebuddy_main.ts",
    "start": "bun run src/remotebuddy_supervisor.ts",
    "preflight": "bun run src/startup/preflight.ts"
  },
  "dependencies": {
    "protocol": "workspace:*",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "@types/bun": "latest"
  },
  "private": true
}
