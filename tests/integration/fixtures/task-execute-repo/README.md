# Task Execute Fixture

This repository fixture provides a deterministic target for integration tests.
It intentionally contains only static documentation so integration tests can
exercise WorkerPals behaviors without touching the real workspace tree.
