warning: in the working copy of 'apps/workerpals/src/backends/miniswe/miniswe_executor.py', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'apps/workerpals/src/backends/openhands/openhands_executor.py', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'apps/workerpals/src/backends/shared/executor_base.py', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'apps/workerpals/src/workerpals_main.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'configs/backend.toml', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/apps/workerpals/src/backends/miniswe/miniswe_executor.py b/apps/workerpals/src/backends/miniswe/miniswe_executor.py[m
[1mindex 95b8be9..32c16c2 100644[m
[1m--- a/apps/workerpals/src/backends/miniswe/miniswe_executor.py[m
[1m+++ b/apps/workerpals/src/backends/miniswe/miniswe_executor.py[m
[36m@@ -33,6 +33,7 @@[m [mif str(_SHARED_DIR) not in sys.path:[m
 from executor_base import ([m
     config_get,[m
     emit,[m
[32m+[m[32m    executor_debug_log,[m
     executor_log,[m
     is_no_tool_calls_error,[m
     log_agent_messages,[m
[36m@@ -168,7 +169,7 @@[m [mdef _run_miniswe_task([m
     executor_log(f"{LOG_PREFIX} Starting mini-swe-agent execution in {repo}")[m
     executor_log(f"{LOG_PREFIX} Model: {model_name}, base_url: {base_url or '(default)'}")[m
     executor_log(f"{LOG_PREFIX} Timeout: {timeout_ms}ms ({timeout_minutes}min)")[m
[31m-    executor_log(f"{LOG_PREFIX} Instruction: {to_single_line(instruction, 300)}")[m
[32m+[m[32m    executor_debug_log(f"{LOG_PREFIX} Instruction: {to_single_line(instruction, 300)}")[m
 [m
     # Pre-run baseline so we can tell whether *anything* changed even if the model/tooling is flaky.[m
     baseline_changes = set(summarize_git_changes(repo))[m
[36m@@ -231,7 +232,7 @@[m [mdef _run_miniswe_task([m
 [m
                 # Log what the agent did[m
                 if hasattr(agent, "messages") and agent.messages:[m
[31m-                    executor_log(f"{LOG_PREFIX} Agent message history ({len(agent.messages)} messages):")[m
[32m+[m[32m                    executor_debug_log(f"{LOG_PREFIX} Agent message history ({len(agent.messages)} messages):")[m
                     log_agent_messages(agent.messages, LOG_PREFIX)[m
                 log_git_status(repo, LOG_PREFIX)[m
                 last_exc = None[m
[1mdiff --git a/apps/workerpals/src/backends/openhands/openhands_executor.py b/apps/workerpals/src/backends/openhands/openhands_executor.py[m
[1mindex 77678cf..551bfe8 100644[m
[1m--- a/apps/workerpals/src/backends/openhands/openhands_executor.py[m
[1m+++ b/apps/workerpals/src/backends/openhands/openhands_executor.py[m
[36m@@ -22,6 +22,7 @@[m [mif str(_SHARED_DIR) not in sys.path:[m
 [m
 from executor_base import ([m
     emit,[m
[32m+[m[32m    executor_debug_log,[m
     executor_log,[m
     is_no_tool_calls_error,[m
     is_truthy_env,[m
[36m@@ -346,7 +347,7 @@[m [mdef _run_openhands_task([m
             agent = Agent(llm=llm, tools=tools)[m
 [m
         conversation = Conversation(agent=agent, workspace=repo)[m
[31m-        executor_log(f"{LOG_PREFIX} Instruction: {to_single_line(instruction, 300)}")[m
[32m+[m[32m        executor_debug_log(f"{LOG_PREFIX} Instruction: {to_single_line(instruction, 300)}")[m
         conversation.send_message(_build_user_message(instruction, timeout_ms))[m
         if supplemental_guidance:[m
             for guidance in supplemental_guidance:[m
[36m@@ -373,20 +374,20 @@[m [mdef _run_openhands_task([m
             else:[m
                 raise[m
 [m
[31m-        executor_log(f"{LOG_PREFIX} Agent execution completed.")[m
[32m+[m[32m        executor_debug_log(f"{LOG_PREFIX} Agent execution completed.")[m
         # Log conversation events if the SDK exposes them[m
         try:[m
             events = getattr(conversation, "events", None) or getattr(conversation, "get_events", lambda: None)()[m
             if events:[m
[31m-                executor_log(f"{LOG_PREFIX} Conversation events ({len(events)}):")[m
[32m+[m[32m                executor_debug_log(f"{LOG_PREFIX} Conversation events ({len(events)}):")[m
                 for i, event in enumerate(events[:30], 1):[m
                     if isinstance(event, dict):[m
                         action = event.get("action") or event.get("type") or "unknown"[m
                         args = event.get("args") or {}[m
                         path = args.get("path") or args.get("command") or ""[m
[31m-                        executor_log(f"{LOG_PREFIX}   Event {i}: {action} {to_single_line(str(path), 120)}")[m
[32m+[m[32m                        executor_debug_log(f"{LOG_PREFIX}   Event {i}: {action} {to_single_line(str(path), 120)}")[m
                     else:[m
[31m-                        executor_log(f"{LOG_PREFIX}   Event {i}: {to_single_line(str(event), 150)}")[m
[32m+[m[32m                        executor_debug_log(f"{LOG_PREFIX}   Event {i}: {to_single_line(str(event), 150)}")[m
         except Exception:[m
             pass[m
         log_git_status(repo, LOG_PREFIX)[m
[1mdiff --git a/apps/workerpals/src/backends/shared/executor_base.py b/apps/workerpals/src/backends/shared/executor_base.py[m
[1mindex c76b5ff..f760fb2 100644[m
[1m--- a/apps/workerpals/src/backends/shared/executor_base.py[m
[1m+++ b/apps/workerpals/src/backends/shared/executor_base.py[m
[36m@@ -83,6 +83,16 @@[m [mdef executor_log(message: str) -> None:[m
     sys.stdout.flush()[m
 [m
 [m
[32m+[m[32mdef _debug_enabled() -> bool:[m
[32m+[m[32m    return os.environ.get("WORKERPALS_DEBUG", "").strip().lower() in {"1", "true", "yes"}[m
[32m+[m
[32m+[m
[32m+[m[32mdef executor_debug_log(message: str) -> None:[m
[32m+[m[32m    """Like executor_log but only emits when WORKERPALS_DEBUG=1."""[m
[32m+[m[32m    if _debug_enabled():[m
[32m+[m[32m        executor_log(message)[m
[32m+[m
[32m+[m
 def fail(summary: str, stderr: Optional[str] = None, exit_code: int = 1) -> int:[m
     """Emit a failure result and return the exit code."""[m
     emit({"ok": False, "summary": summary, "stderr": stderr or "", "exitCode": exit_code})[m
[36m@@ -404,13 +414,13 @@[m [mdef log_git_status(repo: str, log_prefix: str = "[Executor]") -> None:[m
         )[m
         lines = [l for l in (status.stdout or "").splitlines() if l.strip()][m
         if lines:[m
[31m-            executor_log(f"{log_prefix} Git status after execution:")[m
[32m+[m[32m            executor_debug_log(f"{log_prefix} Git status after execution:")[m
             for line in lines[:30]:[m
[31m-                executor_log(f"  {line}")[m
[32m+[m[32m                executor_debug_log(f"  {line}")[m
         else:[m
[31m-            executor_log(f"{log_prefix} Git status: clean (no changes)")[m
[32m+[m[32m            executor_debug_log(f"{log_prefix} Git status: clean (no changes)")[m
     except Exception as exc:[m
[31m-        executor_log(f"{log_prefix} Git status failed: {exc}")[m
[32m+[m[32m        executor_debug_log(f"{log_prefix} Git status failed: {exc}")[m
 [m
     try:[m
         diff = subprocess.run([m
[36m@@ -419,15 +429,20 @@[m [mdef log_git_status(repo: str, log_prefix: str = "[Executor]") -> None:[m
         )[m
         diff_lines = [l for l in (diff.stdout or "").splitlines() if l.strip()][m
         if diff_lines:[m
[31m-            executor_log(f"{log_prefix} Git diff stat:")[m
[32m+[m[32m            executor_debug_log(f"{log_prefix} Git diff stat:")[m
             for line in diff_lines[:20]:[m
[31m-                executor_log(f"  {line}")[m
[32m+[m[32m                executor_debug_log(f"  {line}")[m
     except Exception:[m
         pass[m
 [m
 [m
 def log_agent_messages(messages: list, log_prefix: str = "[Executor]", max_chars: int = 200) -> None:[m
[31m-    """Log a summary of agent message history (works with miniswe's message format)."""[m
[32m+[m[32m    """Log a summary of agent message history (works with miniswe's message format).[m
[32m+[m
[32m+[m[32m    Only emits output when WORKERPALS_DEBUG=1.[m
[32m+[m[32m    """[m
[32m+[m[32m    if not _debug_enabled():[m
[32m+[m[32m        return[m
     step = 0[m
     for msg in messages:[m
         if not isinstance(msg, dict):[m
[36m@@ -447,20 +462,20 @@[m [mdef log_agent_messages(messages: list, log_prefix: str = "[Executor]", max_chars[m
                     fn = tc.get("function") or {}[m
                     name = fn.get("name") or "unknown"[m
                     args_raw = str(fn.get("arguments") or "")[:80][m
[31m-                    executor_log(f"{log_prefix} Step {step} (tool_call): {name}({args_raw})")[m
[32m+[m[32m                    executor_debug_log(f"{log_prefix} Step {step} (tool_call): {name}({args_raw})")[m
             continue[m
 [m
         # Tool response[m
         if role == "tool":[m
             name = str(msg.get("name") or msg.get("tool_call_id") or "tool")[m
             excerpt = to_single_line(content, max_chars)[m
[31m-            executor_log(f"{log_prefix} Step {step} (tool_result/{name}): {excerpt}")[m
[32m+[m[32m            executor_debug_log(f"{log_prefix} Step {step} (tool_result/{name}): {excerpt}")[m
             continue[m
 [m
         # Assistant or user text[m
         excerpt = to_single_line(content, max_chars)[m
         if excerpt:[m
[31m-            executor_log(f"{log_prefix} Step {step} ({role}): {excerpt}")[m
[32m+[m[32m            executor_debug_log(f"{log_prefix} Step {step} ({role}): {excerpt}")[m
 [m
 [m
 # ─── Payload parsing ────────────────────────────────────────────────────────[m
[1mdiff --git a/apps/workerpals/src/workerpals_main.ts b/apps/workerpals/src/workerpals_main.ts[m
[1mindex 7d4bd1b..f5f39ce 100644[m
[1m--- a/apps/workerpals/src/workerpals_main.ts[m
[1m+++ b/apps/workerpals/src/workerpals_main.ts[m
[36m@@ -774,8 +774,10 @@[m [masync function workerLoop([m
             ? (stream: "stdout" | "stderr", line: string) => {[m
                 const cleaned = sanitizeJobLogLine(line);[m
                 if (!cleaned) return;[m
[31m-                // Print executor logs locally so they appear in test/daemon output[m
[31m-                console.log(`[${stream}] ${cleaned}`);[m
[32m+[m[32m                // Print executor logs locally when debug is enabled[m
[32m+[m[32m                if (process.env.WORKERPALS_DEBUG === "1") {[m
[32m+[m[32m                  console.log(`[${stream}] ${cleaned}`);[m
[32m+[m[32m                }[m
 [m
                 // Drop high-frequency terminal progress redraw spam; keep meaningful lines.[m
                 if (isNoisyProgressLine(cleaned)) return;[m
[1mdiff --git a/configs/backend.toml b/configs/backend.toml[m
[1mindex 46b7eba..5801c7e 100644[m
[1m--- a/configs/backend.toml[m
[1m+++ b/configs/backend.toml[m
[36m@@ -15,6 +15,7 @@[m [mshared_passthrough = [[m
   "GH_TOKEN",[m
   "GIT_TOKEN",[m
   "PUSHPALS_REPO_PATH",[m
[32m+[m[32m  "WORKERPALS_DEBUG",[m
 ][m
 [m
 [backends.openhands][m
[1mdiff --git a/tests/integration/test_workerpals_e2e.py b/tests/integration/test_workerpals_e2e.py[m
[1mindex 85619b0..8671924 100644[m
[1m--- a/tests/integration/test_workerpals_e2e.py[m
[1m+++ b/tests/integration/test_workerpals_e2e.py[m
[36m@@ -879,7 +879,7 @@[m [mdef main():[m
             print("==============================\n")[m
 [m
             worker_id = f"e2e-{backend}-{int(time.time() * 1000) % 100000000:08d}"[m
[31m-            worker_env = {"WORKERPALS_EXECUTOR": backend}[m
[32m+[m[32m            worker_env = {"WORKERPALS_EXECUTOR": backend, "WORKERPALS_DEBUG": "1"}[m
             worker_cmd = [[m
                 "bun",[m
                 "run",[m
